use xdg;
use gtk::prelude::*;
use gtk::{cairo, gdk};
use gtk::{ApplicationWindow, Button, Fixed};
use gio::prelude::*;
use glib::prelude::*;

// #[derive(Copy, Clone, Debug)]
// pub struct Entry<T, Y> {
//     type: T,
//     name: Y,
// }

pub fn RunRs() {
    // get the xdg shit here

}
