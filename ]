use gtk4 as gtk;
use gtk::{Application, ApplicationWindow, gio::AppInfo, Shortcut, SearchBar, EventControllerKey};
use crate::utils::AppField;

pub fn input_handling(application: &Application, window: &ApplicationWindow) {
    // select specific app, then fetch the exec
    let event_controller = gtk::EventControllerKey::connect_key_pressed(&self, gtk::gdk::Key::Return);

}

